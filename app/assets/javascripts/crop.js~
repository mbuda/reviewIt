(function() {
  $('#jcrop_target').Jcrop(){
    onChange: showCoords,
    onSelect: showCoords,
    minSize: [200, 400]
  });
  function showCoords(c) {
    console.log('x:', c.x, ' y:', c.y, ' w:', c.w, ' h:', c.h);
  };
})();
